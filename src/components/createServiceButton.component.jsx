import React, { Component } from "react";

class CreateServiceButton extends Component {
  render() {
    return (
      <div>
        Botón que redirige a la página con el formulario de creación de servicio.
      </div>
    );
  }
}

export default CreateServiceButton;
